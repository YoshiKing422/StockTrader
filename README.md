# StockTrader
A lightweight single-page app that shows current stock info and a 7-day chart, with a fast local autocomplete/prediction dropdown for tickers/company names. Uses Yahoo Finance endpoints (via a CORS proxy) and Chart.js for visualization.
